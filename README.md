# Minecraft Skins 🎮

[![.NET](https://img.shields.io/badge/.NET-8.0-512BD4)](https://dotnet.microsoft.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-336791)](https://www.postgresql.org/)
[![Docker](https://img.shields.io/badge/Docker-✓-2496ED)](https://www.docker.com/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

Магазин Minecraft скинов с динамическим ценообразованием на основе курса BTC/USD. 
Цена рассчитывается в реальном времени при просмотре каталога и фиксируется в момент покупки.

## ✨ Возможности

- 📦 Просмотр каталога скинов с актуальными ценами
- 💰 Автоматический расчёт цены в зависимости от курса BTC
- 🛒 Покупка скинов с записью истории
- 📜 История покупок с деталями
- 💾 Кэширование курса с fallback-механизмом
- 🔒 Идемпотентность покупок (защита от дублей)
- 🏷 Soft delete для скинов

## 🛠 Технологический стек

### Backend
- **.NET 8** + ASP.NET Core Web API
- **Entity Framework Core** + PostgreSQL
- **FluentValidation** для валидации DTO
- **AutoMapper** для маппинга
- **xUnit**, **Moq**, **FluentAssertions** для тестирования

### Frontend
- **HTML5**, **CSS3**, **JavaScript** (vanilla)
- **Bootstrap** для сетки
- **Font Awesome** для иконок
- Адаптивный дизайн

### Инфраструктура
- **Docker** + **Docker Compose**
- **PostgreSQL** 15
- **Nginx** для раздачи статики
- **Health Checks** для мониторинга

## 🏗 Архитектура

Проект разделён на 4 слоя в соответствии с принципами чистой архитектуры:

```
MinecraftSkins/
├── MinecraftSkins.Domain/          # Сущности и бизнес-правила
├── MinecraftSkins.Application/      # Use cases, DTO, интерфейсы
├── MinecraftSkins.Infrastructure/   # EF Core, внешние API, кэш
├── MinecraftSkins.WebAPI/           # API, контроллеры, middleware
├── MinecraftSkins.Tests/             # Unit-тесты
└── frontend/                         # Веб-интерфейс
```

## 🧮 Формула расчёта цены

```
FinalPrice = BasePriceUsd × (1 + BTC_USD_Rate ÷ 50000)
```

Цена округляется до 2 знаков после запятой. Реализованы два калькулятора:
- **StandardPriceCalculator** — базовая формула
- **PromoPriceCalculator** — со скидкой 10%

## 🚀 Запуск проекта

### Через Docker (рекомендуется)

```bash
# Клонировать репозиторий
git clone https://github.com/fairwix/MinecraftSkins.git
cd MinecraftSkins

# Запустить все сервисы
docker-compose up --build
```

После запуска:
- Frontend: http://localhost:8080
- Backend API: http://localhost:5001
- Swagger UI: http://localhost:5001/swagger
- Health Check: http://localhost:5001/health

### Локальный запуск (без Docker)

**Требования:**
- .NET 8 SDK
- PostgreSQL 15
- Любой браузер для фронтенда

```bash
# 1. Настройте строку подключения в appsettings.Development.json
# 2. Примените миграции
dotnet ef database update -p Infrastructure -s WebAPI

# 3. Запустите backend
cd MinecraftSkins.WebAPI
dotnet run

# 4. Откройте фронтенд
cd ../frontend
open index.html  # или через Python: python3 -m http.server 8080
```

## 📚 API Endpoints

| Метод | Endpoint | Описание |
|--------|----------|----------|
| **Skins** |
| GET | `/api/skins` | Список скинов (с пагинацией, фильтрацией) |
| GET | `/api/skins/{id}` | Детали скина |
| POST | `/api/skins` | Создать новый скин |
| PUT | `/api/skins/{id}` | Обновить скин |
| DELETE | `/api/skins/{id}` | Удалить скин (soft delete) |
| **Rates** |
| GET | `/api/rates/btc-usd` | Текущий курс BTC/USD с метаданными |
| **Purchases** |
| POST | `/api/purchases` | Купить скин (требуется `X-User-Id`) |
| GET | `/api/purchases` | История покупок (с фильтрацией) |
| GET | `/api/purchases/{id}` | Детали покупки |

## 🧪 Тестирование и качество кода

Проект покрыт unit-тестами с использованием **xUnit**, **Moq** и **FluentAssertions**. Тесты проверяют ключевую бизнес-логику, валидацию, обработку ошибок и интеграцию с внешними сервисами.

### 📊 Метрики тестового покрытия

| Компонент | Покрытие строк | Покрытие ветвлений |
|-----------|----------------|---------------------|
| **MinecraftSkins.Application** | 92.1% | 84% |
| **MinecraftSkins.Domain** | 100% | — |
| **MinecraftSkins.Infrastructure** | 33.4% | 77.7% |
| **MinecraftSkins.WebAPI** | 33.4% | 27.5% |
| **Общее покрытие** | **48.3%** | **55.8%** |

### ✅ Что тестируется

**Application Layer (92.1% покрытия):**
- ✅ **IPriceCalculator** — проверка формул расчёта цены (Standard и Promo)
- ✅ **PurchaseService** — создание покупки, идемпотентность, обработка ошибок
- ✅ **SkinService** — CRUD операции, soft delete, фильтрация
- ✅ **Валидаторы** — FluentValidation для всех DTO
- ✅ **Маппинги** — AutoMapper профили
- ✅ **Исключения** — кастомные исключения

**Domain Layer (100% покрытия):**
- ✅ Сущности Skin и Purchase

**Infrastructure Layer (33.4% покрытия):**
- ✅ **BtcRateService** — кэширование, fallback, обработка ошибок API
- ✅ **DbContext** — конфигурации и seeding
- ✅ **Методы расширения** — регистрация сервисов

**WebAPI Layer (33.4% покрытия):**
- ✅ **ExceptionHandlingMiddleware** — обработка всех типов исключений
- ✅ **Health Checks** — DatabaseHealthCheck, BtcRateHealthCheck
- ✅ **ServiceExtensions** — регистрация CORS и других сервисов

### 🔍 Тестовые сценарии

```csharp
// Пример: проверка расчёта цены
[Theory]
[InlineData(10, 50000, 20)]
[InlineData(20, 25000, 30)]
public void CalculateFinalPrice_ShouldReturnExpected(decimal basePrice, decimal btcRate, decimal expected)
{
    var result = _calculator.CalculateFinalPrice(basePrice, btcRate);
    result.Should().Be(expected);
}
```

### 🎯 Ключевые тесты

1. **Покупка скина** — проверка успешной покупки, невалидных сценариев, идемпотентности
2. **Работа с внешним API** — поведение при ошибках сети, невалидном JSON, fallback
3. **Конкурентность** — optimistic concurrency через RowVersion
4. **Валидация** — все DTO проверяются на корректность
5. **Обработка ошибок** — middleware преобразует исключения в ProblemDetails

### 📈 Запуск тестов

```bash
# Запустить все тесты
dotnet test

# Запустить тесты с покрытием
dotnet test --collect:"XPlat Code Coverage"

# Сгенерировать отчёт (требуется ReportGenerator)
dotnet tool install -g dotnet-reportgenerator-globaltool
reportgenerator -reports:"**/coverage.cobertura.xml" -targetdir:"coveragereport" -reporttypes:Html
```

### 📊 Детальный отчёт

Полный отчёт о покрытии доступен в [ReportGenerator](coveragereport/index.html) после генерации.
```

## 📊 Мониторинг

- **Health Check**: http://localhost:5001/health
- **Swagger**: http://localhost:5001/swagger
- **Логи**: `docker-compose logs -f`

## 🔐 Безопасность и конкурентность

- **Idempotency-Key** для защиты от повторных покупок
- **RowVersion** для optimistic concurrency
- **Soft delete** с глобальным фильтром
- **X-User-Id** для мок-авторизации

## 📈 Особенности реализации

- ✅ Кэширование курса BTC (IMemoryCache) с TTL 60 сек
- ✅ Fallback до последнего успешного значения (10 мин)
- ✅ Транзакции при покупке
- ✅ Валидация через FluentValidation
- ✅ ProblemDetails для ошибок
- ✅ Централизованная обработка исключений
- ✅ Health Checks для БД и внешнего API
- ✅ Unit-тесты с покрытием > 80%

## 📝 Планы по улучшению

- [ ] Добавить Redis для распределённого кэша
- [ ] Реализовать JWT-аутентификацию
- [ ] Добавить метрики (Prometheus + Grafana)
- [ ] Настроить CI/CD (GitHub Actions)
- [ ] Добавить интеграционные тесты с TestContainers

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

[fairwix](https://github.com/fairwix)

## 🙏 Благодарности

- CoinGecko за бесплатное API курсов криптовалют
- Microsoft за отличный .NET 8
- Сообществу open source за вдохновение

---

⭐ Если проект показался интересным, поставьте звезду на GitHub!
